# Legacy Port Mechanisms

This page has been consolidated into
[Old Projects Transfer Audit](../../../docs/OLD_PROJECTS_TRANSFER_AUDIT.md).

The live mechanism summary is generated by:

```powershell
python scripts\legacy_port_mechanisms.py --policy configs\legacy_port_policy.json --out reports\legacy_port_mechanisms.json --markdown-out reports\legacy_port_mechanisms.md
```

Do not add new duplicate status here. Use the canonical transfer audit for
human-readable interpretation and `reports/legacy_port_mechanisms.json` for
machine-readable state.
