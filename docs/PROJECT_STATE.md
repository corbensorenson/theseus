# Project State

Last consolidated: 2026-07-29 UTC.

This is the bounded current-wall page. The machine contract is
`configs/roadmap_implementation_matrix.json`; the forward plan is `roadmap.md`; history
remains in Git, immutable reports, and `docs/archive/`.

## North Star

Build a private, locally trained assistant that Corben uses daily, serves with zero
external inference, improves through governed evidence, and drives accepted live-teacher
share toward zero. Public benchmarks are calibration only.

## Current Verdict

- **Finite pre-long-run acceleration closure:** the cross-domain residual audit is GREEN
  across MLX/Metal, CPU/Rust, ANE/Accelerate/Metal, data staging, checkpoint/replay, memory,
  disk, thermals, joined wall, the Kimi K3 candidates, and execution custody. Compiled FP32
  MLX with microbatch four, width quantum 64, AdamW, separate Q/K/V, SwiGLU, and sequential
  unscaled residuals remains selected among the tested eligible routes. The exact step-11,416
  model/AdamW/RNG aliases and 37-manifest prospective lineage from step 9,048 replay green;
  the pre-anchor chain remains explicitly unavailable. Candidate guards now have zero fixed
  available-memory reserve and use predicted exhaustion plus causal process limits; disk
  custody is derived from two complete checkpoint transactions rather than a round-number
  floor. This is finite execution-selection evidence, not a fastest-possible, capability,
  utility, or scientific-falsification claim. The long-run hold remains installed pending
  the content-addressed replacement freeze. A separately implemented independent audit has
  now re-hashed every residual-audit input, reconstructed all prospective lineage and artifact
  edges, verified fresh-process replay/resource policy, found no consumption row matching the
  current 57M functional freeze identity, and confirmed every candidate negative is scoped.
  It is GREEN and explicitly does not authorize training.
- **Deep-review reconciliation:** the 2026-07-25 source audit accepts the external review's
  authority-surface, evaluator-integrity, benchmark-validity, status-semantics,
  reproducibility, maintainability, and Rust/CUDA correctness criticisms. The dashboard
  currently exposes unauthenticated mutating routes with permissive body handling; autonomy
  defaults teacher/network authority on and can inherit it when the request omits the field;
  the OpenAI shim defaults to no token with wildcard CORS; the local English judge interpolates
  candidate text directly into its instruction prompt; the safety-critical tool route lacks
  an explicit fixed certification floor; Tensor/CUDA cache/device/trace concerns reproduce in
  source; and no public CI workflow is present. Existing control surfaces remain local-only and
  not routine-exposure-ready until the bounded security package closes. Evaluator injection
  and runtime forbidden-field taint defenses are now pre-freeze obligations for `T0A`/`T1`.
- **ASI Stack synchronization:** the committed book revision
  `df0b675230c73e1a749f7d48f3263d126023f692` has 84 chapters, all still at book support state
  `argument`. Theseus's authoritative implementation crosswalk remains pinned to 54 chapters,
  leaving 30 current chapter IDs unmapped. This is a real Phase-19 drift defect, not evidence
  that the 30 concepts need separate services. The next reviewed pin transaction must map all
  84 into existing capability fields with one disposition, maturity/route/evidence state,
  activation gate, owner, tests, residuals, and non-claims. Book changes cannot mutate runtime
  authority; Theseus reports cannot move book support automatically.
- **Architecture consolidation accepted from the book comparison:** preserve a complete
  architecture reference while separating the implementation profile, active runtime, and
  earned evidence. Extend the existing VIEA/SCF/VCM owners with noninheritance, independent
  semantic/epistemic/authority gates, effect-attempt versus observed-effect records, twelve
  shared transition invariants, a minimal trusted-kernel/distrust map, and bounded disposition
  liveness. Do not create another architecture, registry, event database, or report family.
  The joined developmental-intelligence episode and semantic-memory utility work remain
  behavior-gated after `T2`; they cannot delay the current KERC-to-matched-training sequence.
- **Preserved control architecture:** frozen shared encoder-decoder transformer trunk plus
  independent low-rank English, Python, JavaScript/TypeScript, HTML/CSS, and Rust arms.
  Decoder-only dense controls are matched at 57,348,617 active parameters and 67,357,193
  total parameters. The MoECOT path has 57,340,426 active and 67,357,711 total parameters.
- **Training authority:** GREEN for the exact content-addressed campaign after the replacement
  `T0A` package and pre-training readiness gate replay. Optimizer Policy Cards, the bounded Adafactor comparison,
  selected-recipe width transfer, and explicit full-shape SOAP scope removal are complete. The
  factorized control selection is provisional:
  fixed open-vocabulary AR, Semantic-IR control, AdamW, no draft, canonical KV, and the independent
  verifier remain the practical control stack. MTP and dynamic patches retain scoped evidence.
  Full-shape SOAP is removed from the first-campaign finite docket on measured M1/MLX
  native-kernel and amortized-timing grounds without a quality claim; RDC/KERC is scoped out of
  campaign one as `INCONCLUSIVE_EXPERIMENT`, with zero exposure and the K4-K8 successor ladder
  preserved. Generic 1-8 step canaries are not authorized.
  The canonical trainer
  now requires a content-bound candidate lease with an isolated namespace, implementation ids,
  target/phase, step/position/wall/memory/disk budgets, seeds, heldout identity, required checks,
  immutable-control separation, and zero-cheat boundaries. Runtime callbacks enforce host limits;
  final receipts enforce optimizer positions. Accelerator runs additionally require an external
  process watchdog. This is architecture authority, not candidate quality evidence.
- **T0A candidate mechanics:** the MTP owner now exposes conventional independent heads and a
  register-conditioned shared head in addition to the retained rank-one compatibility fixture.
  Both adequate candidates pass bounded MLX gradient-flow, zero-weight ablation, offset-alignment,
  future-loss, shape, and exact-reload checks. A topology-identical, gradient-stopped AR control now
  gives the first honest source-disjoint matched rung across all five arms and three seeds. The run
  is mechanics-GREEN but scientifically `INCONCLUSIVE_EXPERIMENT`: conventional, curriculum, and
  register MTP are each slightly worse on heldout NTP loss after 24 steps, while the admitted corpus
  has no executable functional verifier. No MTP candidate is selected or broadly rejected; the next
  MTP evidence must reuse the fixed pairing on verifier-bearing private tasks. The canonical
  optimizer owner now implements AdamW, Muon with explicit AdamW fallback parameter classes,
  reference-style Schedule-Free AdamW evaluation/training iterate transitions, and factored MLX
  Adafactor with vector/scalar fallback. Four executable digest-bound Policy Cards and the guarded
  `8/8` mechanics shard are GREEN. In the four-way, three-profile, three-seed clipped matched rung,
  Adafactor uses 40,856 state bytes versus AdamW's 3,554,316 and improves aggregate loss 2.34%, but
  a 6.69% English regression misses the frozen 2% weak-arm ceiling. Muon regresses 10.85% and
  schedule-free 57.83%; AdamW remains selected. Its unchanged profile passes exact resume and all
  three isolated `d_model=512` transfer seeds with 73.67% mean private loss improvement. The
  guarded rung peaks at 846 MiB RSS/1,621 MiB inferred unified memory, retains 4,966 MiB
  reclaimable reserve, and adds zero swap. These are bounded engineering dispositions and
  width-transfer evidence, not optimizer-superiority or capability claims. The shared draft ABI
  now implements target-bound Medusa residual heads and an
  EAGLE-style feature predictor, exact target/codec/cache manifest binding, and rejection rollback.
  Both learned modules and a conventional smaller causal draft control pass bounded training and
  exact reload; wrong proposal suffixes cannot change canonical target output. Canonical MLX layer
  KV branches now verify proposal spans in branch-local state, truncate rejected suffix positions,
  commit only accepted prefixes plus the target correction, and preserve exact greedy output.
  Matched source-disjoint accepted-output/resource selection and stochastic sampling equivalence
  remain open.
  The dynamic-byte candidate now passes bounded causal-boundary, gradient, overfit, intervention,
  exact binary round-trip, protected-span, corruption, contraction, and checkpoint-reload mechanics.
  Its one-patch-shifted causal-core mechanics now include a previous-byte-only local decoder;
  intervention proves byte `i` cannot affect its own or earlier logits and sequential inference
  matches teacher-forced local decoding. Canonical corpus/trainer integration
  and matched raw-byte utility/cost comparison are still open;
  a 150-byte mechanics fixture contracting to 60 patch positions is not a compression or capability
  claim.
- **RDC/KERC:** the Semantic-IR owner now exposes an RDC-1.0 relational-state ABI with typed
  dimension axes, exact finite n-ary relation reification through role incidences, branch-safe
  lifecycle custody, proposal-denominator/recall receipts, costed operator selection and
  abstention, reversible query-relative contraction, and content-bound columnar lowering. This is
  representation/governance mechanics only. Exact schema migration/rollback, monitored specialist
  compilation with retained slow path, and sparse factorized NumPy/MLX parity are now mechanics-
  complete. Typed future-unit drafting also passes grammar, typed-pointer, target-authority,
  gradient, intervention, and reload mechanics. A separately parameterized MLX relation proposer
  and qualifier pass bounded learning, source/evidence intervention, label-noninterference, and
  reload mechanics against an observable denominator. Real source-disjoint proposal/qualification,
  real multi-turn K4 utility, canonical-path structured-draft qualification, and complete K5-K7
  behavior remain incomplete. Checkpointed query/KV streaming attention, compact partitions,
  mixed-precision authority, BF16 Adam moments, transactional updates, and zero-authority decoder
  pruning pass exact parity checks. Serial additive backward and exact 128-position token/copy
  chunking pass guarded monolithic loss/gradient parity. The repaired compact-transport canary
  completes 256 steps/20,840 positions with all coverage labels, exact state custody,
  2,624.016 MiB minimum reclaimable reserve, 5,905.156 MiB inferred working set, and zero swap.
  The representative full-objective route is GREEN and advances the mandatory KERC ladder to 2/7:
  q32/k32 checkpointed attention, compact partitions, serial additive FP32 gradient accumulation,
  and exact 128-position token/copy chunks complete the 72,534,757-parameter backward in 21.831
  seconds with 3,041.484 MiB minimum reclaimable reserve, a 3,859.141 MiB inferred working set,
  and 0.125 MiB swap growth. The independent resource auditor binds both parity predecessors,
  recomputes every acceptance predicate, and passes. The superseded q128/k128 attempt that stopped
  after 35.562 MiB swap growth remains diagnostic only. A separate 1,005-token q32/k32 full-objective
  mechanics preflight completes 32 steps with 2,772.219 MiB minimum reserve and zero swap, but does
  not qualify sustained training. The new resource-stress prefix makes the maximum-target retained
  row execute before shuffled sampling can hide it. Sustained 768-token attempts all fail safely on
  the same 716-token/694-target direct-surface row despite thresholded optimizer-state offload,
  16-position loss chunks, or selective q16 attention; 715 tokens fails its explicit stress row too.
  A 648-token q32/k32 route with exact 64-position loss chunks and optimizer-state offload only at
  600+ target positions retains 2,779 rows, including 669 compiler rows, and completes 256
  steps/22,641 positions with 2,903.656 MiB minimum reserve and zero swap. Two terminal launches
  nevertheless fail safely on its 637/609 stress row, including one with 9,424.172 MiB initial
  reclaimable memory. The stress prefix now independently executes both the maximum-target and
  maximum-width rows. At the selected 628-token envelope those rows are 512/496 and 628/408; its
  dual-stress 16-step preflight preserves all coverage with 2,591.875 MiB minimum reserve,
  5,886.984 MiB inferred working set, and zero swap. This is resource/mechanics evidence only.
  Rows above 628 tokens and the maximum 5,866-token positive row remain K7 blockers. The stage probe now independently replays and
  hash-checks the stress prefix. Its outcome-independent 32-row diagnostic finds 31.52% compiler
  accuracy but only 1.01% on singleton compiler tokens; direct-surface is 51.45% overall and 10.40%
  singleton. This supports a longer learning rung, not behavior credit. The stale 4,096-step
  ceiling was replaced by a 6,144-step/43,200-second guarded ceiling for the declared
  423,166-position target. The seed-20260722 terminal run reaches that target at 4,566
  updates/423,227 positions with all 21 coverage labels, exact checkpoint/optimizer custody,
  2,445.547 MiB minimum live reserve, a 6,678.406 MiB inferred working set, zero swap growth,
  and 39,342.743 seconds guarded wall time. The source-disjoint direct panel remains 0/16 with
  sixteen empty outputs. Position completion sampled only 2,129/2,393 retained rows, invalidating
  the prior claim that the position target guaranteed exactly two row exposures. Resource and
  sustained mechanics are green for this 628-token rung; K5 behavior remains
  `INCONCLUSIVE_EXPERIMENT`. Exact replay now confirms all 4,566 updates and localizes the
  checkpoint fault: on sixteen retained rows per objective, renderer is 95.87% top-1 with
  15/16 EOS, core 76.07% with 16/16 EOS, direct surface 56.40% with 14/16 EOS, and compiler
  only 47.97% with 18.78% singleton accuracy and 0/16 EOS. One-row free generation produces
  syntax-valid terminated output for renderer/core/direct but a compiler `FAULT`/
  `kerc_code_decode_fault`. The repaired candidate uses uniform within-objective weights,
  coverage-safe positive-row scheduling, and four position-mass passes. Its hash-bound
  same-seed continuation preserves the exact step-4,566 FP32 master, AdamW, and MLX RNG,
  resets only the kernel data cursor, uses the evidence-selected `3e-4` compiler rate, and writes
  only to a fresh scratch lineage under candidate execution plan `6c351548...f5fc`. The current
  unoverlaid plan computation is `79c2c68b...7be1` and remains RED pending the
  post-implementation generation-architecture freeze refresh. The current focused KERC,
  watchdog, resource-entry, and freeze contract suite passes 147 tests with 17 skips. The
  equal-objective projection covered all rows but wasted
  1,140 updates on the already-strong 108-row renderer. The selected residual-deficit schedule
  preserves the full-row floor and redirects the fixed remaining position budget: it performs
  3,594 updates/423,242 positions, samples all 2,393/2,393 rows, and gives
  renderer/core/direct/compiler 159/1,006/1,098/1,331 updates. This removes 981 renderer
  updates, adds 191 compiler updates, and saves 967 optimizer steps versus equal mass.
  `rdc_kerc_k5_scheduler_projection_compiler_deficit_weighted_coverage_floor_seed_20260722.json`
  is `75f971b2...9717`; it is schedule-only evidence and grants no behavior credit.
  The initial selected-plan launch then caught an unoverlaid/candidate plan-hash mismatch with
  zero swap; the migration now recomputes the stable candidate hash across two scratch namespaces.
  A second launch exposed 278 MiB of redundant common-random initialization before exact state
  replacement. That path is removed: exact continuation now skips the warm-trunk/common-reference
  initialization, writes zero reference files, and keeps exact model/optimizer/RNG custody.
  The optimized q32/chunk64 attempt used 52 KiB scratch metadata, completed optimizer step 1,
  entered step 2, and measured 1,305.745 MiB MLX peak before the guard stopped 185.5 MiB global
  swap. No progress checkpoint was published. A q16/chunk32 variant worsened peak to
  1,408.371 MiB and is rejected for this route; q32/chunk64 is restored. These are resource and
  execution-path findings only, not behavior credit.
  Two guarded scheduler canaries were correctly stopped at
  30.812 and 65.812 MiB global swap growth; their failure grants no scientific negative
  credit and does not justify weakening the 16 MiB ceiling. Live reserve/swap—not a fixed
  host-memory floor—remain authoritative. Two now-superseded equal-mass continuation launches were then admitted
  by the 2,048 MiB live-reserve policy but stopped after 6.837 and 6.644 seconds when global
  swap grew 20.062 and 18.562 MiB. Child RSS was only 71.516 and 72.359 MiB, so neither reached
  model materialization or wrote a checkpoint/training report. Receipts `72c500a0...1055` and
  `15736e2b...1c3f` are host-pressure evidence only.
  The current causal repair no longer asks the whole KERC delta to move at once. It projects the
  exact step-4,566 Adam state by tensor name to the five compiler-stage parameters, retaining
  `step`, `learning_rate`, and both moments without reset: 12 tensors/27,541,516 payload bytes
  versus 244/115,854,200 in the source optimizer. Only the compiler objective receives generator
  loss and sibling/auxiliary heads remain frozen. A declared 128-source-row learnability slice is
  16 times the independently measured eight-row label-cover minimum; after the 628-token bound it
  retains 46 compiler generator rows and all declared K5 constructs. `ABSTAIN` remains explicitly
  deferred to K7 because its shortest selected compiler sequence is 2,289 tokens. The first
  stage-specific contract replay correctly rejected stale global four-objective coverage with
  zero swap (`26c51b39...091`). The repaired CPU stage replay is green; the latest Metal launch
  still stopped before continuation import at 34.5 MiB global swap growth with only 78.234 MiB
  child RSS (`e6d7f3d0...bf8`). Metal initialization is now deferred until bounded CPU
  materialization has been collected. Phase isolation subsequently wrote the exact projected
  optimizer while omitting inactive pretraining memmaps (`a9158f43...8773`). A full-FP32
  single-model attempt raised child RSS to 264.562 MiB and is rejected for this diagnostic
  (`13d69860...e2c`). The current route instead builds frozen weights in BF16, preserves only
  the five compiler trainables as exact FP32, and streams source safetensors in 16 MiB batches.
  It completed model construction and compiler-scope binding at 78.281 MiB RSS, 272.859 MiB
  inferred memory, and zero swap before revealing a now-fixed null-metadata parser bug
  (`9bc96ecb...e2b5`). The first post-fix retry was stopped at 17.312 MiB swap
  (`04e68136...f1a3`), just over the unchanged 16 MiB ceiling. No durable step or behavior
  credit exists yet; any positive mixed-compute compiler delta must be merged into and verified
  against the exact FP32 source lineage before promotion.
  That exact mixed-compute path has now advanced beyond the prior host-pressure wall. Exact
  layer/objective recomputation passes stage-only loss/gradient parity, but was insufficient to
  keep the five-parameter route under the swap ceiling. Because the compiler stage embedding
  enters the frozen trunk, the selected causal intervention explicitly freezes it, detaches the
  trunk, and trains only the four post-trunk compiler output owners. A two-step canary advances
  source step 4,566 to 4,568 with loss 3.969589 -> 3.453082, zero swap, and a 1,338.438 MiB
  inferred peak. A zero-token-authority sampler bug was then removed by excluding 22
  verifier-only rows from generator training while preserving all 24 generator rows and their
  coverage contract.
  The repaired same-seed 32-step rung advances to step 4,598/11,596 positions, samples every
  generator row, reaches final/best sampled losses 3.271693/2.986589, peaks at 1,594.453 MiB,
  and records zero swap. Its exact four-tensor FP32 merge is `685a5d34...abb`; all 235 frozen
  source tensors remain byte-exact. The frozen 16-row source-disjoint evaluation completes with
  zero swap but remains 0/16 and has no nonempty accepted outputs: ten decode faults, five
  incomplete-at-budget faults, and one complete candidate rejected by independent compact-
  transport validation. The bound report is `06338e74...052`. Transactional per-row,
  identity-bound evaluation progress now survives safe watchdog interruption.
  This leaves K5 `INCONCLUSIVE_EXPERIMENT`: 32 output-owner steps are insufficient, but neither
  KERC nor the full compiler stage is falsified. The teacher-forced probe was repaired to honor
  the exact one-objective coverage scope, verifier-row exclusion, reset data cursor, dense
  mutating epoch shuffle, and staged compact transport rather than reconstructing legacy targets.
  It exactly replays 32 updates/11,596 positions and measures 65.32% top-1, 80.41% repeated,
  28.81% singleton, 36.23% Kernel-token, 85.89% pointer/transport-token, and 16/16 KERC-end
  accuracy on sixteen sampled rows (`f2387d7f...f8c`). Termination is not the present owner;
  sparse Kernel semantics are. The selected 64-total-step same-seed rung is now complete:
  step 4,630/23,769 continuation positions, all 24 generator rows, final loss 2.931635,
  2,199.844 MiB inferred peak, and zero swap (`e996e45f...a23`, host
  `bae9ddb9...42b`). Its exact four-tensor FP32 merge is `84b61a49...97e`. The 64-step
  compact diagnostic reaches 67.14% total, 82.33% repeated, 27.70% singleton, 36.84%
  Kernel, 89.18% pointer/transport, and 16/16 KERC-end accuracy. Exposure-ranked selection
  changes seven rows, so only the nine-row overlap is a matched delta; every reported category
  improves on that overlap. The frozen behavior panel nevertheless remains 0/16: thirteen decode
  faults and three complete candidates rejected for two invalid alignment spans and one invalid
  compact transport (`7cebfc9d...57f`). Complete candidates rise from one at 32 steps to three
  at 64, but accepted behavior remains zero. The output-only proxy has therefore localized the
  boundary without qualifying K5.
  The full five-parameter route is now resource-safe without changing its mechanism. Cache-free
  gradient-checkpointed training had retained unused decoder K/V caches as checkpoint outputs.
  Eliding them preserves exact eager/checkpointed loss and gradients; the exact two-step replay
  preserves its checkpoint and optimizer byte-for-byte while reducing MLX peak by 304 MB and
  post-warm step time by about 14%. The formerly failing eight-row replay completes through
  width 624 with 4,884.395 MiB MLX peak and 12.75 MiB swap growth under the unchanged 16 MiB
  ceiling (`3fa41ae8...de3`, host `26793c59...75f2`). Its matched 5,976-token
  source/checkpoint probe finds only seven improved predictions at the inherited `3e-5` floor,
  all pointer/transport, with zero Kernel-token movement.
  A separate exact one-row, 16-update, `3e-4` learnability gate then lowers loss monotonically
  from 3.453700 to 1.944534 with zero swap and a 1,886.219 MiB inferred peak. On the identical
  140-token row, its exact FP32 merge improves total top-1 from 82 to 124, Kernel from 9/25 to
  17/25, pointer/transport from 72/114 to 106/114, and singleton from 29/68 to 54/68. This
  establishes a causal learning-rate owner and intact compiler gradient flow; it is training-row
  sanity only, not K5 or generalization evidence. The exact same-eight-row `3e-4` replay then
  completes with zero swap and improves every post-update sampled loss versus `3e-5`. On the
  identical 5,976-token panel, source / old floor / new rate score 3,916 / 3,923 / 3,962
  correct overall, 888 / 888 / 897 Kernel, 3,012 / 3,019 / 3,049 pointer/transport,
  441 / 443 / 454 singleton, and 3,475 / 3,480 / 3,508 repeated, with EOS 16/16
  throughout.
  The authorized 32-update same-seed population rung is now complete through an exact
  one-step-per-process resume chain. An eight-row proof exactly matches uninterrupted losses,
  checkpoint, optimizer, RNG, cursor, steps, and positions. The full chain samples all 24
  generator rows over 11,900 continuation positions, reaches final/best loss 2.867617, and
  completes the row-23 width that failed after 22 updates in the monolithic process. This confirms
  process-local graph accumulation as the resource owner; the stopped monolithic attempt produced
  no checkpoint and has no learning credit. Successful segments cost 2,687.115 seconds, peak at
  4,652.641 MiB inferred memory, retain at least 2,362.375 MiB reclaimable memory, and observe at
  most 264.75 MiB swap growth. The K5-only 320 MiB burst ceiling is derived from five observed
  64 MiB macOS granules above the reproducible 268.69 MiB burst; the independent 2,048 MiB live
  reserve remains unchanged.
  On the exact same 5,976 targets, source / old floor / eight-update / 32-update score
  3,916 / 3,923 / 3,962 / 4,065 overall, 888 / 888 / 897 / 945 Kernel,
  3,012 / 3,019 / 3,049 / 3,104 pointer/transport, 441 / 443 / 454 / 476 singleton,
  and 3,475 / 3,480 / 3,508 / 3,589 repeated, with EOS 16/16 throughout
  (`32a6f823...def2`). The exact FP32 merge `20995704...3edf` preserves all 234 frozen
  source tensors and all five learned tensors byte-exact.
  The single authorized frozen source-disjoint panel remains 0/16 accepted, but all 16 rows now
  produce complete candidates rather than empty/incomplete outputs. Independent validation rejects
  eight for compact-transport content and eight for compact-transport schema; no invalid output is
  repaired or rewritten (`54f2c73c...abd`, rows `fd9c987b...d2fd`, host
  `d712f8a6...41b3`). K5 remains `INCONCLUSIVE_EXPERIMENT`; its next owner is exact
  transport-schema and semantic-serialization learning on the same seed, not another seed,
  termination weighting, blind step doubling, K4 v2 consumption, or a memory floor. The frozen
  path now costs 128.80 seconds per row and remains a separate K7 throughput owner.
  Private K5 evaluation now uses an exact-once run registry and frozen, content-addressed
  answer-blind surface manifests. It excludes every row identity consumed by an earlier K5
  execution attempt and records the attempt before scoring, so a consumed surface cannot be
  replayed. The first fresh use evaluates 16 new step-4,630 rows: all are complete and none are
  accepted, with nine alignment-span faults, four protected-span replay mismatches, two
  residual-unit fidelity faults, and one remaining compact-transport fault. It takes 1,405.757
  seconds, peaks at 624.125 MiB inferred unified memory, retains at least 4,901.625 MiB
  reclaimable memory, and grows swap by zero (`67afd5dc...2783`; surface
  `c327fba7...5def`; rows `f0e623cf...68d3`; host `1706b6c8...b262`). This is a sharper
  semantic owner, not K5 acceptance.
  An exact audit then finds that byte fallback made the trainer treat logical schema-atom indices
  as encoded model positions. All 12,886 compiler training rows are affected and 202,421 of
  288,052 intended weighted positions are misplaced. The repaired encoder returns exact encoded
  half-open ranges for every logical atom. A same-seed 32-update corrected-schema branch reaches
  step 4,662 and improves matched teacher-forced accuracy from 4,140 to 4,197 of 5,976 tokens,
  but regresses the identical retained-row free generation from an ABI-valid residual length six
  to schema-invalid length four. It is rejected for promotion (`d3f02e9a...83ae`,
  `5bcb09d4...9b43`, `d84baa7a...0740`). The active intervention therefore returns to the
  last ABI-valid step-4,630 checkpoint, restores 1x schema pressure, and applies bounded 4x loss
  only to existing span, numeric-pointer, residual-unit-id, and fidelity target atoms. It must pass
  both matched teacher forcing and retained-row free generation before another exact-once private
  surface is consumed.
  That 32-update semantic branch completes at step 4,662 and improves matched teacher forcing from
  4,140 to 4,185 of 5,976 targets, but the retained-row transport regresses to a nine-field root,
  five-field residual, and empty hierarchy. It is rejected and no fresh surface is consumed
  (`68088085...8116`, merge `dad41cb6...490b`, matched `7b264409...04ac`,
  retained `778da4c0...02e4`). Resource behavior remains healthy: 4,515.078 MiB maximum
  inferred memory, 2,451.609 MiB minimum reclaimable memory, and 0.625 MiB maximum swap growth.
  The next exact owner is narrower: the semantic classifier had also weighted the root ABI version,
  hierarchy chunk index, and unrelated numbers. It now selects only program spans,
  protected-object character bounds, and residual unit-id/fidelity descendants, with tests that
  explicitly exclude the version and hierarchy index. The exact one-step rung reaches step 4,631,
  reduces selected semantic encoded positions from 48,099 to 40,877, adds no values, peaks at
  1,912.312 MiB inferred memory, and grows swap by zero. The exact same retained step-4,630 row
  remains complete, EOS-terminated, transport-valid, and 6/6/3 at root/residual/hierarchy with
  unchanged 291/408 teacher-forced accuracy (`e2cb95db...6765`, merge
  `117396de...230c`, checkpoint `777815b7...0d26`, retained `9dc891bc...1876`).
  The evaluator now preserves population coverage-planning capacity for initial one-step segments
  and supports an explicitly bound retained training-row report without capability credit. The
  focused KERC suite passes 78 tests with five skips. This coherent gate is banked.
  The longer path-qualified replay rejects q16 as roughly five times slower on its first measured
  step while still failing the same third-row swap guard, restores exact q32, and completes all 32
  updates/11,900 positions over 24/24 generator rows. Matched teacher forcing rises from
  4,140 to 4,194 of 5,976 with EOS 16/16, but terminal free generation regresses to invalid
  9/5/0 transport, so the endpoint is rejected. A real custody defect had deleted intermediate
  checkpoints named by the segmented reports; bounded K5 now retains all 32 generations inside
  its existing disk lease. Exact replay localizes the transition: step 27/4,657 is valid 6/6/3,
  while step 28/4,658 becomes 9/3/0 on the second exposure of stage row 14. The selected step-27
  merge is `e1c76ec1...c691`.
  A new fail-closed admitted-training-panel mode then evaluates the exact frozen 16 rows with zero
  capability credit. Step 27 yields 13/16 syntax-valid completions and 12/16 independently valid
  6/6/3 packets; one is 6/6/0 and three hit the token budget. The one authorized new exact-once
  source-disjoint surface still scores 0/16, but all 16 candidates are complete and compact
  transport is no longer the rejection owner. Independent faults are seven alignment-span,
  three protected-span replay, and six residual-unit fidelity failures; no invalid candidate is
  repaired (`1e04d3c8...2011`, surface `afcd5ab8...49fe`, rows `0f8212a6...bbc3`,
  host `91872de7...18e`). K5 remains `INCONCLUSIVE_EXPERIMENT`. v5-v7 loss-only repairs are now
  rejected by exact post-generation semantic evidence. Compiler transport v3 completes the first
  bounded semantic-ownership repair described below and is qualified for one guarded update from
  immutable step 4,657. No new private surface or seed is authorized until admitted semantic
  validity becomes positive without transport regression. The
  101.26-second-per-row fresh decode remains a separate K7 throughput obligation.
  Three process-isolated 128-step KERC/control pairs pass exact common initialization, state custody,
  and zero-swap training, but emit no valid learned-pipeline output. The repaired broad K5 route now
  uses a 4,096-source bounded pool, objective-balanced stratified without-replacement sampling, and
  measured q32/k32 resource policy. Seed 20260722 reaches the full 303,402-position target at 3,097
  updates/303,443 positions with exact checkpoint custody, 2,511.531 MiB minimum live reserve, a
  7,542.281 MiB inferred peak, and zero swap. Its valid embedded and independent source-disjoint
  panels both remain 0/16 with zero complete proposals. Exact retained-row probes localize the fault:
  the best-exposed compiler row is 74.1% top-1 but only 49.0% on Kernel tokens, and a 32-row matched
  256-update control is only 2.17% accurate on singleton compiler targets. Pairwise objective-gradient
  cosines are small and positive, so destructive objective cancellation is not supported. A frozen
  inverse-square-root target-frequency/q16 canary was executed and rejected for a longer rung: it
  improved singleton compiler accuracy by 4.984 points but regressed repeated compiler accuracy by
  6.944 points and cost 2.51x guarded wall time. The q32/no-weight control is restored; a memory-safe
  long-program transport still owns the 97/2,826 compiler-row envelope gap. This exact route remains
  `INCONCLUSIVE_EXPERIMENT`, not a KERC falsification; K4-K7 and an adequately trained K8 verdict
  remain required.
- **Host safety:** the arbitrary 8 GiB launch floor and allocator-peak completion rule are removed.
  Generic guarded work uses a 6,144 MiB launch reserve and 4,096 MiB runtime reserve, while
  source-justified bounded operations may use content-bound launch/live pairs: optimizer
  mechanics and matched selection use 5,120/4,096 MiB and SOAP uses 5,632/4,096 MiB. The measured
  query/KV parity shard no longer inherits that generic floor: its prior 160.297 MiB inferred
  peak binds it to the existing 3,072/2,048 MiB tiny-KERC parity class. A sandboxed diagnostic
  correctly reported no accessible Metal device; the host-Metal replay then passes under the
  smaller envelope after the compiler-stage changes with 3,496.031 MiB
  initial reserve, 3,456.156 MiB minimum reserve, 77.094 MiB inferred working set, and zero swap.
  The refreshed watchdog receipt is `d66f3eba...91f6`.
  Its exact decomposed-objective successor also passes with 79.203 MiB inferred working set and
  zero swap.
  The 72M representative successor's prior receipt is now implementation-stale after the trainer
  change and must be rerun under its evidence-backed 5,120 MiB launch requirement. Do not restore
  an oversized tiny-shard floor or mislabel sandbox Metal denial as an MLX/model failure.
  KERC training candidate uses a 2,048 MiB live reserve and at most 16 MiB new swap. Its training,
  evaluation, teacher-forced probe, and gradient diagnostic now derive separate launch floors from
  their own bound measured peak plus that reserve; lightweight operations no longer inherit the
  training floor. The
  watchdog fails closed if process or VM telemetry is unavailable and writes an explicit
  child-not-started receipt for a preflight refusal. Control-plane telemetry observes MLX only if
  the training process already loaded it; it cannot initialize Metal as a side effect. Pytest now
  ignores top-level MLX modules and treats direct imports plus `pytest.importorskip("mlx.*")` as
  accelerator initialization before test execution, while all
  direct canary entrypoints fail with `ACCELERATOR_WATCHDOG_REQUIRED`. The replacement replay is
  split into 17 qualified guarded shards. Four duplicate native canaries were removed from direct
  scheduling because the generation and policy aggregate gates already execute them. Their exact
  pytest node IDs remain accelerator-marked, so ambient execution still fails closed. The
  aggregate JSON/Markdown outputs are hashed into their shard receipts; stale or modified gate
  reports invalidate replay readiness. The canonical freeze owner runs exact shards one at a time,
  reuses prior exact passing receipts, resumes at the first missing/invalid shard, stops after the
  first refusal/failure, writes each receipt atomically, and derives readiness from complete
  untampered receipts rather than a manually edited status. Static source-shape review now gives every shard an explicit
  risk class and resource basis. Tiny fixtures are capped at 1,024-2,048 MiB process-group
  RSS, preserve their declared operation-specific live reserve, allow at most 16 MiB swapout
  growth, and poll every
  100 ms; no tiny canary inherits the broader 6,144 MiB global emergency ceiling. A separately
  justified representative 72M KERC full-objective shard is governed by its live-reserve and
  swap-growth limits and may run only after
  tiny exact output/loss/gradient parity shards prove checkpointed online-softmax query/KV
  chunking and serial position-chunked objective equivalence. The dependency is structural rather
  than schedule-only: direct representative selection
  fails before launch unless the exact valid parity receipt exists, and the representative receipt
  binds that predecessor's SHA-256. Both shard commands bind their chunking policy directly, and the
  representative report is hash-bound to its guard receipt. Ordinary freeze
  replays no longer use raw `subprocess.run`: each is a serial externally
  watched process group capped at 2,048 MiB RSS, requiring 6,144 MiB before launch and a 4,096 MiB
  runtime reserve, with at most 16 MiB swap growth. Refusal and completion receipts are atomic and
  content-addressed, and replay children never receive accelerator authority. Canonical MoECOT
  training, progress evaluation, and checkpoint migration now fail before MLX import unless the
  script is invoked with `--guarded`; that launcher applies a 5,120 MiB RSS ceiling, a 6 GiB
  launch/4 GiB runtime reserve, 16 MiB swap-growth limit, and a 24-hour wall limit. Candidate
  canaries cannot use the generic launcher and remain restricted to their tighter freeze shards.
  Planning and preregistration parameter accounting are now allocation-free: the canonical model
  owner counts the declared graph analytically instead of importing MLX and instantiating 57M and
  matched-control models during a dry run. CPU-only tests reproduce the frozen MLX counts for the
  54,836,746-parameter trunk, 57,340,426-parameter active arm, 2,504,193-parameter specialist
  scope, 72,534,757-parameter full KERC candidate, and both matched controls. An explicit semantic
  plan migration preserves the immutable step-3000 checkpoint and optimizer identities. The
  preregistration's three sequential 57M-class checkpoint/resume canaries are now a dedicated
  4,096 MiB guarded shard; direct `--execute-canaries` exits before report mutation without
  watchdog authority, and its generated report is hash-bound to the shard receipt. Generation
  and policy aggregate gates likewise refuse before mutating reports when called outside their
  registered shards. Full KERC adequacy and residual-allocation entrypoints now require watchdog
  authority too. Routine registry verification may consume existing governed evidence, but the
  replacement freeze requires the guarded resource-entry and remaining K4-K7 receipts plus an
  adequately trained matched verdict.
- **Data:** 422,334,331 unique model-visible positions, or 7.365 per active parameter.
  The optimizer target is 1,146,808,520 positions over 2.715 planned passes, below the
  frozen four-pass ceiling. Repetition is not counted as unique data. Family totals are
  English 160,196,476; Python 116,242,500; JS/TS 82,769,680; HTML/CSS 17,577,020; and
  Rust 45,548,655.
- **MLX mechanics:** GREEN for the MoECOT active arm and both dense controls. Native MLX
  grouped-query attention is equivalent to explicit KV tiling. The durable shared-trunk
  lineage is now step 3,000 and 22,999,779 optimizer positions, with safetensors model SHA
  `606640cd...5c0e` and AdamW SHA `62e1b52b...5f96`. The adopted microbatch-four route's
  stable same-state three-pair qualification measured 1.89x median/1.88x pooled over eager
  while reducing peak MLX memory from about 8.10 GB to 3.40 GB. All 54,836,746 parameters
  stayed within `2.39e-7` maximum absolute and `8.24e-8` relative-L2 drift. The corrected
  timing contract always excludes the first compile/warmup step rather than whichever step
  happened to be slowest. Existing synchronization points attribute 125.52 seconds (74.5%)
  to the three accumulation microbatches and 42.92 seconds (25.5%) to the final
  forward/backward plus AdamW update, so model compute is the current owner rather than host
  preparation or optimizer-only work. The real 500-update continuation sustained 2,914.2
  positions/second across the checkpoint boundary. A stricter microbatch-eight
  rerun compared all 54,836,746 final parameters after each 24-update route: maximum
  absolute drift was at most `2.38e-7`, relative L2 drift was about `8e-8`, and loss stayed
  within `2e-6`, but speed varied from 1.36x to 1.97x and measured only 1.52x median/1.62x
  pooled with 5.00 GB peak MLX memory. It is semantics-qualified but performance-rejected.
  Full-batch compilation was slower and reached 8.57 GB. The latest
  bf16-compute/fp32-master rerun remained finite with fp32 authority but measured 0.993x
  median/pooled and did not clear its peak-memory gate. It remains
  unadopted on this M1. Deferring all four compiled microbatch synchronizations to the final
  update also preserved all 54,836,746 parameters and exact reported loss, but regressed to
  1.12x median/1.16x pooled and raised compiled peak memory to 7.86 GB. Keep per-microbatch
  synchronization; graph deferral is rejected for this backend/model.
  A bounded size sweep found microbatch six best (`1.86x` median, `1.85x` pooled),
  while sizes five and seven measured `1.64x` and `1.31x`; none cleared `2x`.
  An earlier whole-path fused MLX RoPE trial (`1.24x`) was rejected. A narrower
  training-only `mx.fast.rope` qualification subsequently won all three direct
  manual-versus-fast compiled pairs by 2.3-2.8%, preserved all 54,836,746 parameters
  within `2.38e-7`, reduced peak memory slightly, and preserved 8/8 exact outputs plus
  normalized receipts when both resulting checkpoints were served through the unchanged
  manual reference kernel. Fast RoPE is therefore training-only; using it for serving
  changed 7/8 token paths and remains forbidden. The historical `1.57x`
  projection packet compared eager/separate against compiled/fused-QKV/fused-SwiGLU,
  so it did not isolate either transformation and should not have been described as
  rejecting both. A 2026-07-27 same-compiled production-shape station probe measures
  QKV-only at `1.153x-1.329x` across three guarded fresh processes, while
  SwiGLU-only is slower at `0.8849x`; QKV-only is now an opt-in,
  checkpoint-compatible qualification candidate and SwiGLU fusion remains excluded
  on this M1. A reusable 512-position RoPE basis (`1.64x`) and one monolithic
  accumulation/update graph (`1.53x`) retained bounded update integrity but lost
  end-to-end throughput. A two-microbatch synchronization group then entered
  severe unified-memory pressure during a 12-step exact-checkpoint paired preflight and was
  terminated before a complete result. Its semantics result is inconclusive, but the exact
  implementation is engineering-rejected and absent from the canonical path.
  Fresh bounded owner preflights also rejected separate gradient/update state capture
  (`2,408` positions/second), dormant zero-gradient parameter pruning (`3,128`), and
  fp16-compute/fp32-master (`2,917` versus `2,954` fp32 positions/second with higher peak
  memory). Per-microbatch sequence cropping has no available work because every sampled
  canonical pretraining window and microbatch is already 512 tokens. These are scoped M1,
  MLX 0.29.3 engineering results, not broad falsifications of the techniques.
  The latest 14-minute all-in-one qualifier exposed substantial environment/order drift:
  compiled trials measured 3,093, 3,220, then 2,340 positions/second while eager trials
  measured 1,439, 2,223, then 2,374. Its 1.45x median/1.48x pooled ratio is retained but is
  not stable enough to supersede the earlier repeated result or clear the 2x gate. Peak
  memory remained 3.39 GB compiled versus 8.10 GB eager and all parameter comparisons
  passed. The same hot run measured bf16-compute/fp32-master at 1.128x median/1.130x pooled
  with lower peak MLX memory, still below the preregistered 1.15x adoption floor and
  inconsistent with the prior 0.984x rerun. Mixed precision remains unadopted pending a
  thermally/order-stabilized sustained result.
- **Inference mechanics:** GREEN for the prompt-only direct decoder acceleration. Batched
  beam advance, device-side admissible-logit ranking, and exact pre-forward pruning
  preserved output and normalized receipt identity on 8/8 arm-covered private prompts.
  The latest canonical run reduced aggregate uncached novel-request latency by 8.78x;
  completion and prompt-prefix caches were disabled on both measured routes. The `2x`
  value in the report is the acceptance threshold, not an observed median. Seven of eight
  current outputs still failed closed on byte serialization, so this is a mechanics win,
  not a capability claim. The disabled-until-qualified KERC decoder now shares the optimized beam path
  and passes serial/optimized token-path parity; full KERC pipeline throughput is not claimed.
  A shared-cache indexed-gather variant was exact but only 1.004x pooled. Bounded
  sequence-axis preallocation was only 1.009x on a 512-token stress run. Both were removed.
  The governed resident MLX runtime now loads the registered model once, keeps bounded
  model-local KV prefill and deterministic completion caches, and is wired behind the
  existing OpenAI-compatible service. On one private prompt it preserved exact output/token
  identity, measured 1,895.40x prompt-prefill reuse and 5,482.24x repeated completion reuse,
  and failed closed when production serving was requested. Production remains disabled
  until a direct model-only capability result grants runtime authority.
  The same registered checkpoint now qualifies prompt-length-bucketed cross-request
  prefill/beam advance plus bounded request coalescing. Four distinct private prompts
  retained exact text/state/reason/token identity across three alternating serial/batch
  pairs; the latest direct uncached batching rerun measured 2.23x pooled, while the prior
  three-pair receipt measured 2.30x median/2.18x minimum and concurrent coalescing measured
  2.33x. Peak MLX memory for the four-request comparison
  rose from about 293 MB serial to 458 MB batched. These are novel-request
  throughput figures; completion and prefix-cache gains remain separate reuse evidence.
- **Checkpoint and joined-runtime mechanics:** exact tensor-level qualification over all
  197 shared-trunk tensors found safetensors and the prior NPZ checkpoint identical.
  The registered migration is now committed: the canonical checkpoint is
  `weights.safetensors` (`606640cd...c0e`), its tensor manifest remains
  `5ad2ec6b...1bfa`, all 22,999,779 optimizer positions and the exact AdamW state are
  unchanged, and the old NPZ was removed only after replay and resume validation.
  Safetensors materialized 4.76x faster in the latest alternating three-load comparison; size
  and save time were effectively unchanged. The assistant's
  unchanged governed refresh path now reuses command-, input-, output-, and TTL-bound
  receipts. Runtime refresh no longer reruns the full 15-case deterministic-tool
  qualification: it refreshes the live registry only after binding the exact current tool-card
  set to a clean full qualification receipt. The current canonical run reduced the cold joined
  route from the prior 4.52 seconds to about 1.46 seconds (3.11x), while the latest unchanged
  warm reuse measured about 146.7x over the cold route. Missing, changed, expired, unqualified,
  or boundary-dirty evidence still reruns or fails closed; no qualification case, verifier,
  or governance check is credited as skipped work.
- **External speed-audit disposition:** the suggested beam batching, device-side admissible
  ranking, compiled train step, bf16 trial, and bounded KV preallocation were already present
  or already measured above. The alleged wide-sequence batch-1 collapse does not affect the
  measured shared trunk (fixed width 512); the cited KERC evidence also remained below the
  8K batch-two boundary, with an observed maximum width of 4,242. KERC's roughly 23
  positions/second remains a real separate
  hot path caused by long source-conditioned computation plus learned residual, verifier, and
  decision objectives. KERC is now a `T0A` blocker whose canonical path must be profiled and
  repaired through K4-K8. Isolated kernel wins were tested at full-model scale and did not
  predict end-to-end training wins. The reported `0.955x`/`0.957x` regression belongs to the
  rejected bf16-compute/fp32-master candidate; it is not resident-runtime or cache overhead.
  A new hot-run bf16 result improved to about 1.13x but still missed its adoption floor and
  does not erase the contradictory earlier measurement.
  Cross-request continuous batching was the remaining canonical decode gap and is now
  mechanics-qualified. The audit's `for beam in beams` and `np.asarray(logits)` citations
  refer to retained serial/device-filter-off parity branches, not the default accelerated
  route.
  A 2026-07-23 follow-up review repeated four stale assertions: `mx.compile` is already the
  qualified compiled training owner; uncached direct decode is published at 8.602944x
  aggregate with 2.30x median cross-request batching and 2.33x continuous coalescing;
  bounded KV preallocation was measured and rejected at 1.009x; and the greater-than-8K
  batch-one branch does not touch the fixed-width-512 step-3,000 trunk. The durable 57M
  checkpoint is paused with a stale PID and remains `NOT_EVALUATED`; it is not a live run.
  Consuming the frozen functional surface now would spend exact-once evidence on a knowingly
  incomplete predecessor, so evaluation remains bound to the selected post-`T0A` successor.
  A 2026-07-24 follow-up correctly identified the missing capability outcome and the risk of
  leaving 117 files unbanked, but incorrectly inferred that the factorized bakeoff removed all
  pre-training blockers. KERC K4-K8, guarded replacement replay, and package publication still
  block `T0A`. Once they close, the roadmap forbids another architecture-review cycle from
  displacing the matched five-arm plus dense-active/dense-total campaign and its one frozen
  direct model-only evaluation.
  A 2026-07-25 follow-up again correctly identifies the missing capability outcome and escalates
  the repository risk: `HEAD` is still `11a5bec3` and the worktree has 120 status entries
  (54 tracked, 66 untracked). The claims that nothing changed for eight hours and that the latest
  reports are from July 22 are false; the seed-stamped K5 step-4,662 reports were created July 25.
  `mx.compile` remains the qualified canonical trunk path, and the batch-one branch applies only
  above 8,192 tokens, not to the width-512 trunk or width-628 K5 route. The old bakeoff's
  18.4 GiB maximum-row deferral does not satisfy the current mandatory K4-K8 charter gate:
  bounded K5 has safely re-entered, while 629+ remains an explicit K7 blocker. Bank the next
  coherent tested transaction, finish T0A, then move directly to matched training and frozen
  evaluation; do not start the long campaign early or add another elective review cycle.
- **Learning signal:** the source-disjoint private-development audit now compares step
  2,500 with step 3,000 over 62,743 target positions. Aggregate teacher-forced loss fell
  from 4.198748 to 4.174738 (0.57%). Python, JS/TS, HTML/CSS, and Rust improved; English
  regressed from 4.299828 to 4.318473. This is training feedback, not direct utility.
- **Capability:** still unmeasured at the 57M rung. The historical 10.8M rung remains
  falsified for practical utility: MoECOT, dense-active, and dense-total each scored
  0/160 on the frozen functional surface. That result cannot be relabeled as 57M evidence.
- **Evaluation:** a new source-disjoint, exact-once 160-case private functional contract
  is frozen with 32 cases each for English, Python, JS/TS, HTML/CSS, and Rust. It has zero
  overlap with the v8 packet and zero public payloads, hidden verifier access, fallback
  credit, or authored-template/tool/router credit. Before any case was consumed, its
  immutable identity was explicitly superseded to bind the training-only acceleration
  plan while preserving the candidate packet, serving kernel, verifier, checkpoint tensors,
  and zero consumption. The 70-artifact architecture package now replays 10/10 commands.
  Capability remains `NOT_EVALUATED` until trained checkpoints are scored.
- **KERC:** K0-K3 are banked as high-quality bounded discovery evidence. K4-K8 remain
  incomplete, so the full candidate is `INCONCLUSIVE_IMPLEMENTATION` and blocks the replacement
  freeze. This is not a scientific failure. The old campaign's zero topology/objective/optimizer
  exposure is preserved as control evidence only. The replacement path must complete guarded
  resource entry and K4-K8 rather than defer KERC to a successor.
- **Registry:** GREEN with no routing blockers, missing identities, or governance
  violations. The canonical project state is the registry plus the frozen package, not
  generated report volume.
- **System gates:** the old independently replayed architecture package remains GREEN over 70
  artifacts, 13/13 architecture contracts, and 10/10 replay commands as an immutable control.
  It no longer grants replacement-training readiness. The roadmap gate now separates implementation
  progress from qualification: `wired` and `implemented` are intermediate, and only `qualified`
  can close an architecture prerequisite. It also audits KERC as an ordered, source-bound seven-step
  replacement ladder; an active, missing, reordered, placeholder, or non-GREEN receipt keeps the
  gate RED. The roadmap/matrix intentionally block on Phase 0 reproducibility, KERC resource entry
  and K4-K8, guarded accelerator replay, and replacement-package publication;
  generated gate reports must be refreshed after this worktree contract change. ATTD is RED on source shape:
  `standard_causal_transformer_survival.py` is 7,374 lines against the 7,000-line cap.
  The control plane remains RED from stale historical control reports and unresolved model
  capability/promotion state, not from the acceleration integration.
- **ANE + Metal acceleration:** the intended route is explicit heterogeneous execution, not
  two isolated backends. The public Metal half of an IOSurface bridge is locally GREEN on
  Apple M1 with exact output over 4,096 elements and no intermediate host copy. Output-channel
  partitioning, backward input-gradient reduction, a versioned microbatch pipeline, raw-timing
  ratio selection, and fail-before-checkpoint custody are now executable contracts. The
  compatible baked-weight ANE control is repeatable on this host: four width-512 fresh
  executions compiled and stepped, including a guarded run with zero swap growth. A
  sandboxed `sdpaBwd2` failure is excluded from compiler
  evidence. The tested generic dynamic-weight 64x64 MIL fails, and a small single-blob
  static probe fails before reload can be evaluated, so training is presently limited to
  baked-weight recompilation. State is `INCONCLUSIVE_IMPLEMENTATION`; canonical MLX/Metal
  remains unchanged. Three guarded process-coexistence rounds nevertheless prove useful
  simultaneous work: MLX QKV plus ANE training reaches `1.591x` versus the serial median sum,
  with `1.052x` GPU and `1.027x` ANE median latency, 219.547 MiB maximum inferred unified
  memory, and zero swap growth. This is mechanical overlap, not same-tensor or end-to-end
  acceleration. Core ML
  `.all` is the public automatic-partition inference baseline, while explicit joint
  placement must independently prove overlap, zero-copy integration, parity, replay, and
  sustained wall benefit.
  Same-surface visibility is now independently GREEN: Metal writes 65,536 FP16
  values and ANE consumes that IOSurface generation before any host read; 32,768 outputs
  match the host-populated control bit-for-bit. Metal and ANE then concurrently read the same
  sealed generation into distinct outputs at `1.287489x` versus serial work, still with
  bit-exact ANE output and zero swap. This removes device visibility and shared-read
  concurrency from the wall, but not full-block partition parity, MLX integration, weight
  update custody, or sustained end-to-end gain.
  Baked-weight recompilation is measured and excluded for current M1 production training:
  it occupies `95.9%` of the batch-four-analogue wall and `92.7%` even at an
  update-contract-changing accumulation eight. Static inference remains open; training now
  requires dynamic/persistent weights with exact generation custody.
  A native `512 -> 768` output-channel split is mechanically GREEN and exact against its
  isolated partition controls, with `1.179x–1.524x` concurrent-versus-serial partition
  speedup. Physical concatenation erases the gain (`0.687x–0.820x` versus full Metal), so it
  is excluded. The successor keeps attention-head or MLP-channel partitions device-local
  through the full subgraph and fuses only a hidden-width partial-result sum.
  The resulting exact native decoder block has now been joined and compared directly with
  the compiled MLX control in two alternating rounds. Native means are `10.176` and
  `10.353 ms`; MLX means are `5.809` and `5.537 ms`, leaving native at only `0.552679x`
  pooled and `0.534825x` conservatively. The native batch-1/sequence-128 route is therefore
  closed for production training and compiled MLX remains authoritative.
  Two smaller MLX candidates were also requalified without arbitrary percentage hurdles.
  `MLX_METAL_FAST_SYNCH=1` preserves exact loss and passes the frozen model-state tolerance,
  but its four matched speed ratios are `1.021917`, `1.026620`, `1.001462`, and
  `0.972203`; the current pair reverses direction and grows swap, so ordinary synchronization
  remains selected. Target-window-only output projection is exact for loss and every gradient
  in the small-model proof and reaches `590.485` positions/s against controls at `565.360`
  and `560.908`, but the live checkpoint diverges by `7.489696e-6` in one tensor against a
  frozen `5e-6` absolute tolerance and grows swap. It remains
  `INCONCLUSIVE_IMPLEMENTATION` until its backward path is repaired; a modest measured wall
  gain is not discarded merely for being below a round-number threshold.
- **Evidence-driven host qualification:** the selected compiled FP32 route no longer has a
  fixed launch/runtime reclaimable-memory floor, a round-number disk floor, an unrelated
  wall deadline, or a two-hour/85% sustained gate. The external watchdog derives only the
  termination headroom implied by consecutive measured memory decline and its real
  termination latency. The exact selected route also binds two independently completed
  64-step receipts into a `4,797.484 MiB` finite working-set envelope, preventing a known
  allocation ramp from being extrapolated as an infinite decline; one allocation transient
  or a stable low plateau is not a fault.
  Disk authority is the byte size of two complete live checkpoint transactions
  (`1,316,246,632` bytes at step 3,480). Sustained qualification uses adjacent
  three-replicate thermal windows and stops when their observed pairwise wall-rate
  uncertainty overlaps, or immediately on clear degradation or an available system thermal
  warning. System-wide swap remains diagnostic, process-group memory remains bounded, every
  segment remains transactional and exact-resumable, and no canonical checkpoint is mutated
  by qualification.
  The final clean qualification completed six exact 64-step segments in adjacent replicated
  windows. Joined-throughput medians were `3,042.702` and `3,017.356` positions/s; their
  `0.991670` ratio has a `[0.944840, 1.025765]` replicate interval containing one, with no
  thermal warning. Independent two-segment replay preserved cursor and RNG and matched every
  model/optimizer tensor under the frozen absolute-or-relative contract, with maximum deltas
  `2.3841858e-7` and `3.608875e-9`. All causal guards passed. Host-wide swap grew and is
  recorded as diagnostic under `report_only`; it is not mislabeled as zero. The canonical
  step-3,480 model, AdamW state, RNG, and receipt remain byte-unchanged.
  Qualification is `INCOMPLETE`; this is neither a training nor capability failure.
- **Repository hygiene:** the forward roadmap is now a compact execution map backed by
  the complete machine-readable matrix without deleting an open obligation.
  The 2026-07-23 audit caught a real regression: 1,946,857,145 governed-hot bytes and 6,375
  governed-hot files exceeded the 1 GiB/6,000 policy, and a dry run could overwrite and
  downgrade the cumulative custody manifest. The retention owner now makes dry runs
  side-effect-free, merges only executed outcomes, reconstructs missing custody from verified
  pointers, preserves original pointer metadata, protects renewable canonical views, handles
  binary and list-shaped non-pointer reports, and enforces independent byte and file targets
  with 20% headroom. Reference-aware compaction archived 1,584 historical payloads across
  the cleanup sequence. Full replay is GREEN at 3,698/3,698 generations with zero failures;
  the budget is GREEN at 354,534,094 governed-hot bytes and 4,802 governed-hot files.
  `reports/` still contains 3,689 small pointers by design, so raw directory entry count is
  not confused with the governed-hot set. Checkpoint volume remains
  above its warning target at about 9.70 GB but below its hard ceiling. The superseded
  strict-generator/code-LM route is being decoupled,
  but cannot be honestly retired until its registered successor passes the same functional
  contract or an explicit evidence-preserving demotion is recorded. Phase 0 reproducibility is
  now qualified: exact Python 3.12 CPU/data/MLX profiles bind complete 25/52/62-package closures,
  a fresh locked CPU environment passes the full non-accelerator suite, and the Rust workspace is
  green. The phase remains partial until the ongoing T0A/KERC edits are frozen, independently
  replayed, and rebound in the replacement transaction, and the superseded family is formally
  retired without evidence loss. The current 117-file dirty worktree is an explicit
  reproducibility gap. After the relevant gates pass, bank coherent reviewed checkpoints with
  source/config/report identities; do not treat committing as evidence, and do not fold unrelated
  user changes into a checkpoint.

## Binding Next Action

1. Preserve the independently replayed 70-artifact architecture package unchanged.
2. Preserve the qualified Phase 0 environment and rerun its source-bound gate after changes.
   Do not rebind the provisional replacement transaction until KERC/T0A content is frozen and
   independently replayed; a hash-only refresh is not qualification. Bank coherent reviewed
   checkpoints after validation so the current 117-file worktree is not a single failure domain.
3. Preserve the exact step-3,000 shared-trunk lineage. The compiled microbatch and direct
   decode routes preserve exact reference behavior; direct decode clears its speed gate,
   while compiled training still needs a robust 2x result. Safetensors migration and
   resident prompt/completion reuse and bounded continuous batching are now qualified and
   registered. Continue measuring publication cost and sustained uncached decode without
   changing model math or objective; do not enable production model serving before direct
   utility is positive.
4. Preserve the accepted guarded KERC query/KV, decomposed-objective, representative 1,005-token,
   bounded 648-token, dual-stress 628-token, and terminal 4,566-update/423,227-position receipts.
   Treat the current 0/16 accepted result as `INCONCLUSIVE_EXPERIMENT`, not a KERC negative.
   Exact segmented resume has completed the 32-update same-seed population rung, matched evidence
   improves total, Kernel, pointer, singleton, and repeated predictions without EOS regression,
   and all 16 frozen rows now terminate with complete candidates. The remaining direct-behavior
   wall is independently split 8/8 between compact-transport content and schema invalidity.
   The admitted-training diagnostic has since driven three prospectively bound repairs from the
   selected step-4,657 checkpoint. v5 rejected its `3e-4` update after frozen teacher forcing
   fell 4,194→4,187/5,976 and valid transports fell 12→1/16. v6 used the registered `3e-5`
   minimum: update one preserved 4,194/5,976 and improved the panel to 13 valid 6/6/3
   transports, but update eight retained the same aggregate score while collapsing to 3/16 valid.
   Retained replay places the first failure exactly at update eight, stage index 8 / source index
   15,944 / width 609. Neither rejected endpoint consumed a new private surface.

   The causal defect was loss allocation, not a need for another seed or an arbitrary memory
   threshold: v5/v6 reduced the historical schema-continuation weight from 8 to 1. v7
   restores schema weight 8 and scales the four semantic weights so combined postweight mass
   remains 166,147.9985. Its independent audit covers 2,640 schema positions and 40,877 semantic
   positions and binds the unchanged sampler prefix. The first update exactly preserves frozen
   4,194/5,976 teacher-forced accuracy, EOS 16/16, and 13/13 complete diagnostic transports at
   6/6/3. The full eight-update rung is now complete at optimizer step 4,665. Update four first
   rolled back at 324.438 MiB swap growth, then passed unchanged after host recovery; later
   segments stayed inside the same 2,048 MiB reserve and 320 MiB swap gates. This is evidence
   against an arbitrary host floor.

   v7 is rejected for semantic promotion. Its endpoint gains only one teacher-forced token
   (4,195/5,976) while valid admitted transports fall to 4/16. Exact bisection places the first
   selected-row regression at update seven, stage index 5 / source index 11,242 / width 609.
   A new post-generation independent semantic audit gives no candidate repair or assisted credit.
   The source and v7 update one both score 0/13 semantic passes. v7 changes the fault distribution
   from alignment/schema/protected/fidelity 5/1/1/6 to 5/0/1/7, while all per-family
   teacher-forced counts remain identical: 308/401 alignment, 3/6 protected bounds, 109/112
   fidelity, 393/1,792 unit IDs, and 202/204 schema. No private surface was consumed.

   Compiler transport v3 now fixes the first semantic-ownership defect. Residual unit identity and
   fidelity are owned by the independently built post-compiler residual packet and dedicated
   allocator head, so the compiler wire no longer asks the decoder to reproduce prompt-hidden
   24-hex hashes or duplicate allocator decisions. V3 rejects any compiler-populated allocation,
   independently attaches the canonical packet after accepting the learned program, and assigns
   that attachment zero generation/repair credit. Full admitted-train replay migrates 12,886 of
   12,886 compiler rows, removes 208,224 allocator rows and 35.0182% of compiler wire bytes, keeps
   the frozen code vocabulary/checkpoint shapes, and consumes no evaluation row. The bounded
   128-source stage drops from 118,803 to 74,369 target tokens and now retains 124 rather than 46
   rows under the same 628-token envelope with complete required coverage.

   The v3 plan restores schema weight 8 and uniform weight 4 for its remaining compiler semantic
   owners (alignment spans and protected bounds); compiler residual-ID/fidelity position counts
   are zero. Protocol roundtrip, ownership rejection, zero-credit post-compiler attachment,
   legacy-v2 compatibility, and canonical materialization tests pass.

   The one authorized guarded update is complete at step 4,658 / position 457,228 with zero swap
   growth and an exact five-learned/234-frozen-tensor FP32 merge. Matched v3 teacher forcing moves
   only 3,120→3,123/3,640 singleton-level correct tokens; EOS stays 16/16, schema 203/204,
   alignment 309/401, protected bounds 3/6, and the v3 root token remains 0/16. On the frozen
   admitted-row free-generation panel, 13/16 rows terminate but all 13 emit legacy v2 and fail the
   required v3 ABI; three hit the token budget. This is `LEARNABILITY_SANITY_FAILED`, not semantic
   promotion.

   Exact replay shows the update sampled one of 63 compiler rows. The v3 token begins at mean rank
   678.6875 and -17.379203 logits versus v2; one exposure improves it across all 16 matched rows
   to rank 670.0625 and -17.337442, far from emission.

   The one-row overfit control proves root learnability but fails full recovery. At 16 repeats the
   root is rank 1 / +5.472826 and teacher forcing is 115/120, but raw v3 has 14 root slots. At 32
   repeats teacher forcing reaches 118/120 and +14.645162, but generation loops to 640 tokens.
   The remaining errors include a closing bracket even though the old report says schema 12/12.
   The schema owner was incomplete: it weighted fixed-slot commas but not fixed-container closing
   brackets. The corrected owner raises bounded schema positions 2,640→3,300 with no target,
   vocabulary, row-selection, semantic-position, or tensor-shape change.

   The rerun improves root cardinality from 14 slots to 9 at 16 repeats; at 32, schema is 15/15
   and teacher forcing is 119/120, but raw generation still loops. The last error is precisely
   target index 75 inside a `PBYTE:` token: one base64 fallback byte copied from source is wrong.
   This isolates the next owner. Compiler transport v4 now replaces exact prompt-source copies
   with typed `PSOURCE_BYTES:<start>:<end>` program tokens. Materialization is source-bound,
   fail-closed, and explicitly receives zero generation, repair, or capability credit; non-source
   literals remain learned `PBYTE` targets.

   The full 12,886-row audit covers 25,772 positive and verifier-negative targets, converts
   7,790/9,716 byte literals, retains 1,926, preserves v3 semantics exactly on 25,772/25,772, and
   has zero failures. Exact frozen-vocabulary accounting falls 15,604,850→15,412,318 encoded
   tokens (1.233796%). No evaluation surface or new row was consumed. The audit authorizes only
   the same one-row v4 overfit control.

   That control now passes: loss 3.674020→2.590474, teacher forcing 97/97, EOS 1/1, schema 15/15,
   alignment 8/8, and v4 root rank 1 / +15.606631 over v3. Raw generation emits 96 tokens, stops
   at EOS, exactly matches the 351-character configured v4 target, and passes compact, online, and
   independent semantic validation. Host safety records 1,419.891 MiB maximum inferred unified
   memory, 3,661.031 MiB minimum reclaimable availability, and zero swap growth.

   The run also exposed and closed two evaluator faults: v4 online validation was not passed the
   prompt source, and exact match compared compact v4 output with the legacy object target.
   Population exposure from immutable step 4,658 is now authorized on the same seed and admitted
   training rows. Exact exposure accounting, weakest-row diagnostics, raw admitted-panel validity,
   checkpoint/reload, and source-disjoint behavior remain required. No fresh private surface is
   authorized and this is not a capability claim.
   Preserve the measured K5-only 320 MiB burst ceiling plus independent 2,048 MiB live-reserve
   stop; do not restore an arbitrary memory floor. Use both maximum-target and maximum-width
   prefixes for every longer rung. Continue repairing the 629+
   and maximum-positive-row routes against the live-reserve/swap watchdog; allocator high-water
   is diagnostic and must not become another arbitrary limit.
5. Complete KERC K4 interaction causality, adequately train the K5 learned
   compiler/reasoner/renderer and structured drafting path, close K6 security/state resilience and
   K7 end-to-end optimization, then rerun the matched verdict at a budget justified by the known
   learnability curve. Execute or explicitly scope-remove SOAP, then rebuild and replay the
   replacement freeze.
6. Make the declared pilot/review ladder executable, then train the five language arms
   and both preregistered dense controls through matched successive-halving reviews. Once `T0A`
   is green, no new architecture-review artifact may displace this execution unless a named
   acceptance gate fails.
7. Evaluate all candidates once on the frozen 160-case functional contract without tuning
   from heldout outcomes.
8. Select the practical architecture from direct model-only utility, weak-arm behavior,
   matched compute/data, resource cost, and uncertainty. Routing success, tools, renderers,
   templates, or verifier repair cannot count as model generation.
9. Only after a behavior-positive proposer exists, qualify VCM, STS, search,
   preference/RL, and fast-generation modes through matched causal ablations.
10. Run fresh public calibration only after a material frozen model change; never train on
   public benchmark prompts, tests, solutions, traces, or answer templates.

## Non-Claims

- Mechanics readiness is not learned capability.
- Training loss is not functional utility.
- KERC resource blocking is neither completion nor KERC falsification.
- MoECOT routing or arm selection is not answer correctness.
- Externally generated tokens are never served to users.
- Public benchmarks and frozen heldouts are not training data.

## Canonical Evidence

- `reports/neural_seed_50m_scale_preregistration.json`
- `runtime/standard_causal_transformer_scale_v2/canonical_pretrain_capacity_v2.json`
- `runtime/standard_causal_transformer_scale_v2/stage_metadata_v1.json`
- `reports/standard_causal_transformer_57m_stage_materialization.json`
- `reports/moecot_language_arm_training.json`
- `configs/neural_seed_57m_functional_utility_freeze.json`
- `configs/evaluation_history/neural_seed_v8_candidate_packet.json`
- `reports/pretraining_architecture_freeze_package.json`
- `reports/resource_acceleration_qualification.json`
- `reports/ane_training_feasibility_2026_07_27.json`
- `reports/ane_metal_heterogeneous_plan_2026_07_27.json`
- `reports/ane_metal_same_surface_m1.json`
- `reports/ane_baked_weight_amortization_m1.json`
- `reports/ane_metal_split_linear_m1.json`
- `configs/ane_metal_heterogeneous_execution.json`
- `configs/neural_seed_architecture_review.json`
- `configs/neural_seed_architecture_review_freeze_v5.json`
- `reports/neural_seed_architecture_review_status.json`
- `reports/neural_seed_campaign_controller.json`
- `reports/theseus_artifact_budget_gate_current.json`

## Canonical Checks

```bash
python3 scripts/theseus_project_registry.py --gate
python3 scripts/pretraining_architecture_freeze.py --execute-replays
python3 scripts/roadmap_implementation_gate.py --gate --require-pre-training-ready
# Required after Phase 0 reproducibility closure:
python3 -m pytest -q -m "not accelerator"
cargo test --workspace
```

## Current Wall

`T0A` remains the historical qualified freeze. T1 is transactionally paused at step 11,416
for the operator-requested finite pre-long-run review in
`configs/neural_seed_pre_long_run_review.json`. The campaign stopped only after a complete
64-step transaction and retains exact model, AdamW, RNG, cursor, receipt, child, and host-guard
custody from the prospective step-9,048 anchor. The review does not reopen generic backend work:
it reconciles completed compiled-MLX, auxiliary-phase, BF16, and exact ANE evidence and admits
only Per-Head Muon, AttnRes, and SiTU-GLU as bounded Kimi K3-derived canaries. KERC remains a
protected successor discovery lane rather than campaign-one work or a scientific negative.
Per-Head Muon is complete and not selected: on the exact 6.623M, three-profile, three-seed
rung it regressed mean heldout loss 8.2798%, never reached AdamW final quality, used
1.213938x AdamW training wall, and reached 21.8135% weak-arm regression. Exact checkpoint
and next-update replay passed. This result is scoped to that implementation and regime;
AttnRes and SiTU-GLU were the only remaining Kimi K3 candidates at that point.
Block AttnRes is now complete and not selected as well. Its exact mechanics, migration
refusal, cached decode, checkpoint reload, and next-state replay pass, but the matched rung
regressed mean loss 1.7047%, used 1.149979x joined wall and 1.084348x allocator peak,
required 1.514768x time to control final quality, and reached 7.6733% weak-arm regression.
SiTU-GLU is complete and not selected too. Its exact beta-4/beta-25 implementation is
parameter matched and passes source-bound, near-origin, gradient, migration-refusal,
cached-decode, reload, and next-state replay checks, but regressed mean loss 0.6669%, used
1.147000x joined wall and 1.040047x allocator peak, required 1.158342x time to control
quality, and reached 3.2683% weak-arm regression. All three admitted Kimi K3 candidates
are closed; replacement selection/readiness and independent audits remain.
The last measured execution residual is closed too: checkpoint-compatible fused QKV won
only one of three alternating step-11,416 full-route pairs, reached 0.972096x geometric
and 0.970465x pooled speed, and failed the frozen final-loss/model/optimizer continuity
bounds while preserving exact RNG, batches, cursor, and parameter schema. Separate Q/K/V
remains authoritative. This is scoped to the tested MLX concatenation graph.

T1 is paused at shared-trunk step 11,416 and 87,441,996 of 1,096,734,920
pretraining positions. The live canonical path advanced after the committed T0A freeze, but the
exact step-3,480 payload and a complete immutable step-by-step predecessor chain were not retained.
`configs/neural_seed_t1_lineage_anchor.json` therefore records an explicit prospective anchor at
step 9,048 and the pre-anchor custody gap. Thirty-seven append-only manifests now carry the
prospective chain through step 11,416; the first manifest's initially invalid before-receipt
digest was repaired from the intact archived receipt with the invalid-manifest hash retained,
and the verifier now re-hashes every artifact.
Every later state-changing segment must archive exact
before/after receipts plus child and host-guard identities in the append-only lineage ledger before
another segment can launch. This preserves current work without laundering the missing predecessor
chain into a replay, capability, or support claim.

The security package is parallel but real: it blocks routine dashboard/OpenAI-shim/Hive/LAN
exposure, not MLX training. The 84-chapter reconciliation is also bounded metadata work and may
not expand the active architecture or displace KERC. Before the next long run, bank one coherent
tested transaction without sweeping unrelated user changes into it.
