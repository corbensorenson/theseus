{
  "policy": "project_theseus_local_8b_functional_evaluator_v1",
  "campaign_id": "ASI-THESEUS-FLAGSHIP-01",
  "experiment_id": "L0-002",
  "cohort_id": "L0_real_work_paired_v1",
  "state": "PROSPECTIVELY_BOUND_BEFORE_CANDIDATE_GENERATION",
  "cost_policy": {
    "id": "l0_elapsed_equivalent_seconds_v1",
    "units": "seconds",
    "formula": "(worker_wall_ms + evaluator_verification_wall_ms + human_review_and_repair_minutes * 60000) / 1000"
  },
  "candidate_output_schema": {
    "required_fields": [
      "worker_id",
      "natural_request_sha256",
      "parent_source_commit",
      "patch_unified_diff",
      "proposed_paths",
      "verification_commands",
      "abstained"
    ],
    "maximum_patch_bytes": 262144,
    "maximum_proposed_paths": 16
  },
  "tasks": [
    {
      "opaque_task_id": "task-l0-002-dogfood-dry-run-receipt",
      "family": "repository_maintenance",
      "natural_request": "In scripts/dogfood_trace_event.py, make dry-run reporting precise while preserving its consent and safety boundaries. When every hard gate passes but --execute is absent, return YELLOW with write_blocker execute_not_requested. Use write_blocker capture_disabled_or_consent_missing only when --execute is present but capture or consent still blocks the append. Update the module description to say that writes require both --execute and explicit valid local consent, and update the emitted consent_scope to dogfood_metadata_only_v1. Preserve metadata-only accounting, zero training performed by the logger, zero external inference or teacher calls, zero public-benchmark use, no fallback returns, and no raw user or assistant text. Add focused coverage in tests/test_dogfood_outcome_accounting.py.",
      "parent_source_commit": "7134acfc15e3a94c40af89bf82a2686bc3650e0d",
      "allowed_effect_paths": [
        "scripts/dogfood_trace_event.py",
        "tests/test_dogfood_outcome_accounting.py"
      ],
      "hidden_test_files": [
        {
          "source": "tests/fixtures/core_evidence_repository_stack_development_hidden/dogfood_event_dry_run_contract.py",
          "destination": "tests/functional_hidden/test_dogfood_event_dry_run_contract.py"
        }
      ],
      "acceptance_contract": [
        {
          "criterion": "a non-executing dry run reports its actual blocker",
          "request_quote": "When every hard gate passes but --execute is absent, return YELLOW with write_blocker execute_not_requested.",
          "hidden_test": "tests/functional_hidden/test_dogfood_event_dry_run_contract.py",
          "assertion_marker": "request_contract:execute_not_requested"
        },
        {
          "criterion": "the metadata consent scope advances to version one",
          "request_quote": "update the emitted consent_scope to dogfood_metadata_only_v1",
          "hidden_test": "tests/functional_hidden/test_dogfood_event_dry_run_contract.py",
          "assertion_marker": "request_contract:metadata_scope_v1"
        }
      ],
      "baseline_failure_markers": [
        "request_contract:execute_not_requested",
        "request_contract:metadata_scope_v1"
      ],
      "verification_timeout_seconds": 180
    }
  ],
  "boundaries": {
    "authoritative_target_patch_used": false,
    "target_commits_opened": 0,
    "target_patches_opened": 0,
    "candidate_authored_claims_trusted": false,
    "external_inference_calls": 0,
    "teacher_calls": 0,
    "public_calibration_cases_consumed": 0,
    "D2_cases_consumed": 0,
    "E2_heldout_cases_consumed": 0,
    "automatic_source_of_truth_effects": 0,
    "fresh_capability_credit_allowed": false,
    "ASI_stack_support_state_effect": "none"
  }
}
