plugins {
    id "com.android.application"
}

android {
    namespace "local.projecttheseus.hive"
    compileSdk 35

    defaultConfig {
        applicationId "local.projecttheseus.hive.android"
        minSdk 26
        targetSdk 35
        versionCode 1
        versionName "0.1.0"
    }
}
